/**
 * Índice de Constantes
 * Exporta todas las constantes del sistema desde un único punto
 */

const { ESTADOS, ESTADOS_RUTA_2 } = require('./estados');
const { ROLES, ROLES_RUTA_2 } = require('./roles');
const { ACCIONES } = require('./acciones');

module.exports = {
    ESTADOS,
    ESTADOS_RUTA_2,
    ROLES,
    ROLES_RUTA_2,
    ACCIONES
};
